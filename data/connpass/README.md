# Connpass dataset

`v0n5ai.jsonl` is the event-level dataset. `participation.jsonl` is the person/event relationship.

The first collection target is the current 176-event history shown by the public profile. Keep source URLs and retrieval timestamps on every record.
